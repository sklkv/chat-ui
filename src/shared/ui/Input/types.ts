import { TextField } from "@radix-ui/themes";

// export interface IInputProps extends Omit<TextField.RootProps, "onChange"> {
//   onChange?: (value: string) => void;
// }

export interface IInputProps extends TextField.RootProps {}
